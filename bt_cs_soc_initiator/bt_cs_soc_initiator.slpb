---
parameters:
- name: "build_dir"
constants:
- name: "project_name"
  value: "bt_cs_soc_initiator"
steps:
- task: "create_gbl"
  output: "{{build_dir}}/{{project_name}}.gbl"
  app: "{{build_dir}}/{{project_name}}.out"
